// No-op stub of `server-only` for the Vitest jsdom environment.
// In production, the real `server-only` import is enforced by Next.js
// bundler boundaries — this stub only applies when running tests.
export {};
